import { Layout } from '@/components';

export default function Home() {
	return (
		<Layout>
			<section>
				<h1 className="text-xl uppercase">Welcome to the Homepage</h1>
				<p className="text-lg">Your settings are well configured</p>
			</section>
		</Layout>
	);
}
